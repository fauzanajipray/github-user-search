<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dicoding.faprayyy.consumerapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.dicoding.faprayyy.githubuser.READ_DATABASE"/>
    <uses-permission android:name="com.dicoding.faprayyy.githubuser.WRITE_DATABASE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_action_github"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_action_github"
        android:supportsRtl="true"
        android:theme="@style/Theme.GithubUser">
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity><activity
        android:name=".view.SplashActivity"
        android:theme="@style/SplashScreen">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

    </application>

    <queries>
        <package android:name="com.dicoding.faprayyy.githubuser" />
    </queries>
</manifest>